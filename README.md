# crlab2

# Задача 1
1. Код разделен на 3 файла:
- StackSolver.h
- StackSolver.cpp
- main.cpp

2. Исправления:
- исправлено возвращаемое значение при пустом стеке (было 1, стало 0)
- добавлен метод Pop (по условию задачи)
- исправлен вывод адреса вершины
- улучшена читаемость кода

3. Переименования:
- x -> Data
- next -> Next
- top -> Top
- push -> Push
- show -> Print
- showAddress -> PrintAddress
- getTopValue -> GetTop
- isEmpty -> IsEmpty

4. Дополнительно:
- добавлен header файл с описанием задачи
- код разделен на модули
- добавлены комментарии FIX_ME

# Задача 2
1. Код разделен на 3 файла:
- QueueSolver.h
- QueueSolver.cpp
- main.cpp

2. Исправления:
- добавлен ввод очереди с клавиатуры
- добавлена защита от удаления из пустой очереди
- добавлен вывод элементов очереди
- добавлен вывод адресов начала и конца очереди
- логика работы с очередью вынесена из main в функции

3. Переименования:
- PQueue -> TQueue
- front -> Head
- last -> Tail
- data -> Data
- next -> Next

4. Дополнительно:
- добавлен header файл с описанием задачи
- добавлены комментарии FIX_ME
- код приведен к более аккуратному стилю

  # Задача 3
  1. Код разделен на 3 файла:
- ListSolver.h
- ListSolver.cpp
- main.cpp

2. Исправления:
- список оставлен реализованным через классы
- STL полностью убран
- добавлен стандартный набор методов: Add, Clear, Print
- исправлена и упрощена логика вставки значения M перед каждым вторым элементом
- добавлен отдельный метод получения последнего элемента списка
- добавлено корректное освобождение памяти

3. Переименования:
- data -> Data
- next -> Next
- head -> Head
- add -> Add
- print -> Print
- clearList -> Clear
- getLastNode -> GetLast
- insertBeforeEverySecond -> InsertBeforeEverySecond

4. Дополнительно:
- код разделен на header / cpp / main
- в header добавлены комментарии
- добавлены комментарии FIX_ME

# Задача 4

1. Код разделен на 3 файла:
- DoubleListSolver.h
- DoubleListSolver.cpp
- main.cpp

2. Исправления:
- добавлен header файл с описанием задачи
- список приведен к более аккуратной реализации через классы
- добавлен стандартный набор методов: PushBack, DeleteNode, Print, Clear
- исправлен вывод пустого списка
- добавлена проверка результата после удаления
- реализован дружественный интерфейс через operator<<

3. Переименования:
- data -> Data
- next -> Next
- prev -> Prev
- head -> Head
- push_back -> PushBack
- printList -> Print
- removeSides -> RemoveEqualNeighbors

4. Дополнительно:
- код разделен на header / cpp / main
- добавлены комментарии FIX_ME
- добавлен friend-интерфейс для вывода списка

# Задача 5
1. Код разделен на 3 файла:
- DequeSolver.h
- DequeSolver.cpp
- main.cpp

2. Исправления:
- заполнение дека переведено на использование конструктора от итераторов
- вывод элементов переведен на использование итераторов
- добавлен обратный вывод через rbegin и rend
- добавлен дружественный интерфейс operator<<
- вставка пяти средних элементов оставлена одним вызовом insert
- ввод с клавиатуры, случайный ввод и чтение из файла вынесены в отдельные методы

3. Переименования:
- FuncInsert -> InsertMiddleFive
- FuncKeyboard -> ReadFromKeyboard
- FuncRandom -> ReadRandom
- FuncFile -> ReadFromFile
- D -> Data внутри класса

4. Дополнительно:
- код оформлен через класс
- добавлены комментарии в header
- добавлены комментарии FIX_ME
